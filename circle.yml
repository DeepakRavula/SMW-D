dependencies:
  override: echo "Skipping build. Moving to tests ..."

test:
  override:
    - if [[ $CIRCLE_BRANCH != "master" ]] ; then echo "Skipping tests. Moving to deployment" ; fi

deployment:
  staging:
    branch: master
    commands:
      - ssh smwkristin@107.170.32.79 'cd /home/smwkristin/smw && git pull'
      - ssh smwkristin@107.170.32.79 'cd /home/smwkristin/smw && console/yii migrate/up --interactive=0'